
简单工厂模式: 根据传来的参数,返回相应的类型。

工厂方法模式：只生产同一种类的产品，当产品创建过程复杂的时候且多处需要用到时

抽象工厂模式：生产不同产品族和产品等级的产品。

单例模式：保证系统中有且只有一个实例。

原型模式：根据一个模板，克隆出N多个实例。

代理模式：被代理类不做事，由代理类来做,通常会扩展被代理对象方法的功能。

委派模式：老板不干活，给下面人干,只求结果不会扩充原方法的功能。

策略模式：干同一件事，有很多种方法途径。

模板模式： 固定的一套业务流程，中间有些环节是一样的。有些是有差异的。

适配器模式：为了兼容原有的规范，且原有的规范没有一个统一的接口，并且实现新的规范。

装饰着模式：对原有功能的扩展。

观察者模式：学生进入课堂（注册）听老师上课（被观察者发布消息，观察者接收消息）